# WaCS Photonincs documentation

This repository hosts the documentation for our group.

## Setup

We use [Zensical](https://zensical.org/docs/get-started/). To preview locally, fork the repository, clone it, install Zensical, and run the server:

```
pip install zensical
zensical serve
```